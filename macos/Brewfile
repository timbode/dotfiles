# macos/Brewfile — GUI apps for a fresh Mac. Installed via `brew bundle`.
# Add a line and re-run `bash bootstrap.sh mac` to install more.
# Find a cask name with:  brew search --cask <name>
brew "dockutil"          # used by macos/dock.sh to rebuild the Dock layout
cask "visual-studio-code"
cask "iterm2"
cask "rectangle"
cask "firefox"
cask "logseq"
cask "claude"
